# MCP 配置功能测试指南

## 快速测试步骤

### 1. 启动应用

```bash
# 开发模式
pnpm tauri dev
```

### 2. 打开 MCP 配置

1. 按 `Alt+Space` 打开应用
2. 点击右上角设置按钮（三层图标）
3. 在左侧菜单点击"MCP 配置"

### 3. 测试添加服务器

#### 测试案例 1：天气服务器

**输入：**
- 服务器名称：`weather-api`
- 命令：`uvx`
- 参数（每行一个）：
  ```
  mcp-server-weather@latest
  ```
- 环境变量：
  ```
  API_KEY=test_key_123
  LOG_LEVEL=info
  ```
- 自动批准工具：`get_weather, get_forecast`

**预期结果：**
- ✅ 服务器添加成功
- ✅ 显示在服务器列表中
- ✅ 状态点为绿色（已启用）
- ✅ 统计信息更新（总数 +1，已启用 +1）

#### 测试案例 2：文件系统服务器

**输入：**
- 服务器名称：`filesystem`
- 命令：`uvx`
- 参数：
  ```
  mcp-server-filesystem@latest
  --root=/Users/username/Documents
  ```
- 环境变量：（留空）
- 自动批准工具：`read_file, list_directory`
- ☑️ 创建后禁用此服务器

**预期结果：**
- ✅ 服务器添加成功
- ✅ 状态点为灰色（已禁用）
- ✅ 统计信息更新（总数 +1，已禁用 +1）

### 4. 测试服务器管理

#### 测试启用/禁用

1. 点击服务器右侧的开关图标
2. **预期结果：**
   - ✅ 状态点颜色改变
   - ✅ 统计信息更新
   - ✅ 显示"配置已保存"提示

#### 测试查看详情

1. 点击服务器卡片
2. **预期结果：**
   - ✅ 展开显示详细信息
   - ✅ 显示命令、参数、环境变量、自动批准工具
   - ✅ 再次点击可折叠

#### 测试删除服务器

1. 点击删除图标（垃圾桶）
2. 确认删除
3. **预期结果：**
   - ✅ 显示确认对话框
   - ✅ 服务器从列表中移除
   - ✅ 统计信息更新
   - ✅ 配置文件更新

### 5. 测试表单验证

#### 测试必填字段

1. 点击"添加服务器"
2. 不填写任何内容，直接点击"添加"
3. **预期结果：**
   - ✅ 显示错误提示："服务器名称和命令不能为空"

#### 测试重复名称

1. 添加一个名为 `test-server` 的服务器
2. 再次添加同名服务器
3. **预期结果：**
   - ✅ 覆盖原有配置（或显示警告）

### 6. 测试配置持久化

1. 添加几个服务器
2. 关闭应用
3. 重新打开应用
4. 打开 MCP 配置
5. **预期结果：**
   - ✅ 所有服务器配置保留
   - ✅ 启用/禁用状态保留

### 7. 测试配置文件

#### 查看配置文件

```bash
cat ~/.local/share/com.lewen.my-utools/mcp.json
```

**预期内容：**
```json
{
  "mcpServers": {
    "weather-api": {
      "name": "weather-api",
      "command": "uvx",
      "args": ["mcp-server-weather@latest"],
      "env": {
        "API_KEY": "test_key_123",
        "LOG_LEVEL": "info"
      },
      "disabled": false,
      "autoApprove": ["get_weather", "get_forecast"]
    }
  }
}
```

#### 手动编辑配置

1. 手动编辑配置文件
2. 重启应用
3. 打开 MCP 配置
4. **预期结果：**
   - ✅ 显示手动添加的配置

### 8. 测试边界情况

#### 空状态

1. 删除所有服务器
2. **预期结果：**
   - ✅ 显示空状态提示
   - ✅ 显示图标和提示文字
   - ✅ 统计信息显示 0

#### 长文本

1. 添加服务器，使用很长的名称和参数
2. **预期结果：**
   - ✅ 文本正确显示（可能截断或换行）
   - ✅ 不影响布局

#### 特殊字符

1. 在环境变量中使用特殊字符（=, ", \n）
2. **预期结果：**
   - ✅ 正确解析和保存
   - ✅ 显示时正确转义

### 9. 测试 UI 响应

#### 按钮状态

- ✅ 悬停时按钮高亮
- ✅ 点击时有反馈
- ✅ 禁用按钮不可点击

#### 动画效果

- ✅ 展开/折叠有平滑动画
- ✅ 添加/删除有过渡效果
- ✅ 提示消息自动消失

### 10. 测试错误处理

#### 无效 JSON

1. 手动编辑配置文件，写入无效 JSON
2. 打开 MCP 配置
3. **预期结果：**
   - ✅ 显示错误提示
   - ✅ 不崩溃

#### 文件权限

1. 修改配置文件为只读
2. 尝试保存配置
3. **预期结果：**
   - ✅ 显示权限错误
   - ✅ 不崩溃

## 测试检查清单

### 功能测试
- [ ] 添加服务器
- [ ] 编辑服务器（通过删除重新添加）
- [ ] 启用/禁用服务器
- [ ] 删除服务器
- [ ] 查看服务器详情
- [ ] 配置持久化

### UI 测试
- [ ] 统计信息正确显示
- [ ] 空状态正确显示
- [ ] 表单验证工作正常
- [ ] 成功/错误提示显示
- [ ] 按钮悬停效果
- [ ] 展开/折叠动画

### 数据测试
- [ ] 配置文件正确保存
- [ ] 配置文件正确加载
- [ ] JSON 格式正确
- [ ] 特殊字符处理

### 错误处理
- [ ] 必填字段验证
- [ ] 无效 JSON 处理
- [ ] 文件权限错误处理
- [ ] 网络错误处理（如果适用）

## 已知问题

1. **服务器状态**：目前只显示启用/禁用状态，不显示实际运行状态
2. **配置验证**：不验证命令是否存在
3. **环境变量**：不支持多行值

## 改进建议

1. 添加配置导入/导出功能
2. 添加服务器连接测试
3. 添加配置模板
4. 添加服务器日志查看
5. 添加批量操作

---

**测试完成后，请在此记录结果：**

- 测试日期：
- 测试人员：
- 通过项目：__ / __
- 失败项目：
- 备注：
