# 快速测试指南

## ✅ 问题已修复

修复了 App.vue 文件中的重复 `</style>` 标签问题。

## 🚀 启动应用

```bash
pnpm tauri dev
```

## 🎨 预期效果

### 1. 窗口外观
- ✅ 窗口有 18px 圆角
- ✅ 背景是半透明毛玻璃效果
- ✅ 窗口有柔和的阴影
- ✅ 整体呈现 macOS 原生质感

### 2. 搜索栏
- ✅ 半透明背景
- ✅ 聚焦时有蓝色光晕效果
- ✅ 右侧设置按钮悬停时上移

### 3. 应用卡片
- ✅ 悬停时上移 2px
- ✅ 悬停时阴影增强
- ✅ 点击时有缩放反馈
- ✅ 流畅的动画过渡

### 4. 插件卡片
- ✅ 渐变背景图标
- ✅ 悬停时上移 3px
- ✅ 更强的阴影效果
- ✅ 点击缩放反馈

### 5. 滚动条
- ✅ macOS 原生样式
- ✅ 半透明滚动条
- ✅ 悬停时变亮
- ✅ 圆角 4px

## 🎯 测试步骤

### 1. 基本功能
```bash
# 启动应用
pnpm tauri dev

# 应该看到：
# - 应用图标出现在顶部菜单栏
# - 不在 Dock 显示
# - 窗口初始隐藏
```

### 2. 快捷键测试
- 按 `Alt+Space` → 窗口显示
- 再按 `Alt+Space` → 窗口隐藏
- 按 `Esc` → 窗口隐藏

### 3. 托盘菜单测试
- 左键点击托盘图标 → 窗口切换
- 右键点击托盘图标 → 显示菜单
- 测试菜单项：
  - "设置" → 打开设置界面
  - "显示/隐藏" → 窗口切换
  - "退出" → 应用退出

### 4. UI 交互测试
- 悬停在应用卡片上 → 卡片上移 + 阴影增强
- 悬停在插件卡片上 → 卡片上移 + 阴影增强
- 点击搜索框 → 蓝色光晕出现
- 悬停在设置按钮上 → 按钮上移

### 5. 插件测试
- 点击"剪贴板历史" → 打开剪贴板插件
- 复制一些文本 → 应该在插件中显示
- 点击"备忘快贴" → 打开备忘录插件
- 创建一条备忘录 → 应该保存成功

### 6. 搜索测试
- 输入 "剪贴板" → 显示剪贴板插件推荐
- 输入 "memo" → 显示备忘录插件推荐
- 输入应用名称 → 显示应用搜索结果

## 🐛 故障排除

### 问题：窗口不透明
**原因**：可能需要重启应用
**解决**：
```bash
# 完全退出应用
# 重新运行
pnpm tauri dev
```

### 问题：毛玻璃效果不明显
**原因**：需要在窗口后面有内容才能看到模糊效果
**解决**：
- 在应用窗口后面打开其他窗口
- 移动应用窗口到不同位置

### 问题：托盘图标不显示
**原因**：可能是图标文件问题
**解决**：
- 检查 `src-tauri/icons/icon.png` 是否存在
- 查看终端日志

### 问题：剪贴板不记录
**原因**：需要打开剪贴板插件才会开始监控
**解决**：
1. 打开剪贴板插件
2. 复制一些文本
3. 应该能看到记录

## 📊 性能检查

### CPU 使用率
- 空闲时：< 1%
- 剪贴板监控：< 2%
- 动画时：< 5%

### 内存使用
- 初始：~50MB
- 运行中：~80MB
- 有数据：~100MB

## ✨ 设计亮点

### 1. 毛玻璃效果
- 真实的半透明背景
- 模糊和饱和度增强
- 完美的 macOS 质感

### 2. 流畅动画
- 所有交互都有反馈
- macOS 标准缓动
- 时长适中（150-250ms）

### 3. 精致细节
- 1px 细边框
- 适度的透明度
- 柔和的阴影
- 圆润的圆角

### 4. 层次分明
- 通过透明度区分层级
- 悬停时增强层次感
- 阴影营造深度

## 🎉 完成！

如果所有测试都通过，说明应用已经完美运行！

享受你的 macOS 风格应用吧！🚀
