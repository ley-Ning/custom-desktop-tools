# 使用指南

## 快速开始

### 1. 启动应用

```bash
# 开发模式
pnpm tauri dev

# 构建应用
pnpm tauri build
```

### 2. 唤出窗口

按下 `Alt+Space`（默认快捷键）唤出应用窗口。

### 3. 搜索应用

在搜索框中输入应用名称，实时显示搜索结果。

### 4. 启动应用

- 使用 `↑` `↓` 键选择应用
- 按 `Enter` 键启动选中的应用
- 或直接点击应用图标

## 主要功能

### 应用搜索

#### 模糊搜索
输入应用名称的任意部分，例如：
- 输入 "saf" 可以找到 "Safari"
- 输入 "code" 可以找到 "Visual Studio Code"

#### 首字母缩写
输入应用名称的首字母，例如：
- 输入 "vsc" 可以找到 "Visual Studio Code"
- 输入 "ps" 可以找到 "Photoshop"

### 最近使用

主界面显示最近使用的 8 个应用，点击即可快速启动。

### 市场精选

主界面展示精选的插件应用，点击可查看详情或安装。

## 设置功能

### 打开设置

点击搜索栏右侧的设置按钮（三层图标）进入设置界面。

### 修改快捷键

1. 进入设置界面
2. 找到"搜索框快捷键"设置项
3. 点击"修改"按钮
4. 按下新的快捷键组合（例如：`Cmd+Space`）
5. 点击"保存"按钮

### 超级面板设置

- **启用超级面板**：开关按钮
- **鼠标按键弹出**：选择长按鼠标右键/左键/中键
- **长按毫秒响应**：拖动滑块调节 100-500ms

### 其他功能开关

- **英文快速翻译**：开启后可快速翻译英文
- **语音交互**：开启后支持语音输入
- **自动固定**：开启后窗口自动固定

## 插件应用市场

### 打开市场

1. 进入设置界面
2. 点击左侧导航栏的"插件应用市场"

或者：
1. 在主界面点击"更多插件..."卡片

### 浏览插件

- **搜索插件**：在搜索栏输入关键词
- **查看精选**：浏览精选插件列表
- **切换排行榜**：点击排行榜标签查看不同排行

### 安装插件

1. 找到想要安装的插件
2. 点击插件卡片右侧的下载按钮
3. 等待安装完成
4. 插件会出现在左侧已安装列表

### 管理插件

- **查看已安装**：左侧列表显示所有已安装插件
- **卸载插件**：点击插件的更多按钮选择卸载
- **更新插件**：检查并更新插件到最新版本

## 快捷键列表

### 全局快捷键

| 快捷键 | 功能 |
|--------|------|
| `Alt+Space` | 唤出/隐藏窗口（可自定义） |

### 应用内快捷键

| 快捷键 | 功能 |
|--------|------|
| `↑` | 向上选择 |
| `↓` | 向下选择 |
| `Enter` | 启动选中的应用 |
| `Esc` | 清空搜索 / 隐藏窗口 / 退出设置 |

## 窗口操作

### 移动窗口

拖拽顶部搜索栏可以移动窗口位置。

### 隐藏窗口

- 按 `Esc` 键
- 点击窗口外部
- 启动应用后自动隐藏

### 窗口特性

- 无边框设计
- 始终置顶显示
- 不可调整大小
- 失去焦点自动隐藏

## 常见问题

### Q: 如何更改快捷键？

A: 进入设置 → 搜索框快捷键 → 点击修改 → 按下新的快捷键 → 保存

### Q: 为什么搜索不到某些应用？

A: 当前只扫描 `/Applications` 目录下的 `.app` 应用。如果应用安装在其他位置，暂时无法搜索到。

### Q: 如何卸载插件？

A: 进入插件市场 → 左侧已安装列表 → 点击插件的更多按钮 → 选择卸载

### Q: 窗口无法移动怎么办？

A: 确保拖拽的是顶部搜索栏区域，而不是搜索输入框或按钮。

### Q: 快捷键冲突怎么办？

A: 进入设置修改为其他快捷键组合，避免与系统或其他应用冲突。

## 技巧和建议

### 提高搜索效率

1. 使用首字母缩写快速定位
2. 记住常用应用的关键词
3. 利用最近使用快速访问

### 自定义快捷键

建议使用不常用的组合键，例如：
- `Option+Space`（默认）
- `Cmd+Shift+Space`
- `Ctrl+Option+Space`

### 插件推荐

根据需求安装以下插件：
- **剪贴板**：管理剪贴板历史
- **聚合翻译**：多平台翻译
- **OCR 文字识别**：图片文字识别
- **JSON 编辑器**：格式化 JSON

## 开发相关

### 添加新功能

1. 修改 `src/App.vue` 添加前端逻辑
2. 修改 `src-tauri/src/lib.rs` 添加后端逻辑
3. 使用 `pnpm tauri dev` 测试
4. 使用 `pnpm tauri build` 构建

### 调试技巧

- 使用浏览器开发者工具调试前端
- 查看终端输出调试后端
- 使用 `console.log` 输出调试信息

### 贡献代码

1. Fork 项目
2. 创建功能分支
3. 提交代码
4. 发起 Pull Request

## 更新日志

### v0.1.0 (当前版本)

- ✅ 基础应用搜索和启动
- ✅ 全局快捷键支持
- ✅ 应用图标提取
- ✅ 最近使用展示
- ✅ 市场精选展示
- ✅ 完整设置界面
- ✅ 插件应用市场 UI

### 未来计划

- [ ] 插件系统实现
- [ ] 云同步功能
- [ ] 多语言支持
- [ ] Windows/Linux 支持
- [ ] 主题自定义
- [ ] 插件开发 SDK


## 剪贴板历史插件

### 打开插件

在主界面点击"剪贴板历史"卡片。

### 功能说明

#### 自动监听
- 插件会自动监听系统剪贴板变化
- 每次复制内容都会自动保存到历史记录
- 最多保存 100 条历史记录
- 重复内容不会重复保存

#### 搜索历史
1. 在搜索框输入关键词
2. 实时过滤匹配的历史记录
3. 支持内容搜索

#### 复制内容
- **鼠标点击**：点击历史记录即可复制
- **键盘操作**：
  - `↑` `↓` 选择记录
  - `Enter` 复制选中的记录
  - `Esc` 关闭插件

#### 收藏功能
1. 点击记录右侧的星标按钮
2. 收藏的记录会标记为黄色
3. 点击顶部星标按钮只显示收藏的记录

#### 删除记录
1. 点击记录右侧的删除按钮
2. 确认后删除该记录

#### 清空历史
1. 点击顶部垃圾桶按钮
2. 确认后清空所有历史记录

### 使用技巧

1. **快速复制**：使用键盘导航比鼠标更快
2. **收藏常用**：将常用内容收藏，方便查找
3. **定期清理**：定期清空不需要的历史记录

## 备忘快贴插件

### 打开插件

在主界面点击"备忘快贴"卡片。

### 功能说明

#### 创建备忘录
1. 点击顶部"+"按钮
2. 输入标题和内容
3. 添加标签（可选）
4. 点击"保存"按钮

#### 编辑备忘录
1. 点击备忘录卡片
2. 修改标题、内容或标签
3. 点击"保存"按钮

#### 搜索备忘录
1. 在搜索框输入关键词
2. 支持搜索标题、内容和标签
3. 实时过滤匹配的备忘录

#### 置顶功能
1. 点击备忘录右上角的图钉按钮
2. 置顶的备忘录会显示在列表顶部
3. 再次点击取消置顶

#### 标签管理
1. 在编辑器底部输入标签名称
2. 点击"添加"按钮或按 Enter
3. 点击标签上的 × 删除标签
4. 标签可用于分类和搜索

#### 删除备忘录
1. 点击备忘录右上角的删除按钮
2. 确认后删除该备忘录

### 使用技巧

1. **善用标签**：为备忘录添加标签，方便分类管理
2. **置顶重要**：将重要备忘录置顶，快速访问
3. **定期整理**：定期删除不需要的备忘录
4. **搜索查找**：使用搜索功能快速找到需要的内容

### 使用场景

- **临时笔记**：快速记录临时想法
- **代码片段**：保存常用代码片段
- **待办事项**：记录待办事项
- **密码备份**：临时保存密码（注意安全）
- **文章草稿**：记录文章草稿

## 插件快捷键

### 剪贴板历史
- `↑` - 向上选择
- `↓` - 向下选择
- `Enter` - 复制选中的记录
- `Esc` - 关闭插件

### 备忘快贴
- `Esc` - 关闭插件或返回列表

## 数据存储

### 剪贴板历史
- 存储位置：`~/.local/share/com.lewen.my-utools/clipboard.json`
- 最大记录数：100 条
- 数据格式：JSON

### 备忘录
- 存储位置：`~/.local/share/com.lewen.my-utools/memos.json`
- 无数量限制
- 数据格式：JSON

### 数据备份

建议定期备份数据文件：

```bash
# 备份剪贴板历史
cp ~/.local/share/com.lewen.my-utools/clipboard.json ~/backup/

# 备份备忘录
cp ~/.local/share/com.lewen.my-utools/memos.json ~/backup/
```

## 常见问题

### Q: 剪贴板历史不自动记录？

A: 确保插件已打开并在后台运行。插件会每秒检查一次剪贴板变化。

### Q: 备忘录数据会丢失吗？

A: 数据保存在本地 JSON 文件中，不会丢失。建议定期备份。

### Q: 可以导出备忘录吗？

A: 当前版本暂不支持导出功能，计划在未来版本中添加。

### Q: 剪贴板历史占用空间大吗？

A: 最多保存 100 条记录，占用空间很小（通常小于 1MB）。

### Q: 支持图片剪贴板吗？

A: 当前版本只支持文本内容，图片支持计划在未来版本中添加。
