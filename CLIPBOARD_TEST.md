# 剪贴板功能测试指南

## ✅ 已修复的问题

1. **剪贴板内容不显示** - 已修复
   - 打开插件时立即检查剪贴板
   - 添加了调试日志
   - 改进了重复检测逻辑

2. **增强的功能**
   - 支持文本显示
   - 支持图片预览（准备中）
   - 支持文件信息显示（准备中）
   - 文件大小格式化显示

## 🧪 测试步骤

### 测试 1: 基本文本复制
1. 复制这段文本：`Hello, this is a test!`
2. 按 `Alt+Space` 打开应用
3. 点击"剪贴板历史"插件
4. 应该能看到刚才复制的文本

### 测试 2: 多次复制
1. 依次复制以下内容：
   - `第一条测试内容`
   - `第二条测试内容`
   - `第三条测试内容`
2. 打开剪贴板插件
3. 应该能看到 3 条记录，最新的在最上面

### 测试 3: 搜索功能
1. 在搜索框输入关键词
2. 应该能过滤出包含关键词的记录

### 测试 4: 收藏功能
1. 点击某条记录的星标按钮
2. 点击顶部的"只看收藏"按钮
3. 应该只显示收藏的记录

### 测试 5: 复制到剪贴板
1. 点击任意历史记录
2. 应该自动复制到剪贴板并关闭插件
3. 粘贴验证内容是否正确

## 🐛 调试信息

打开应用后，在终端查看日志：
```bash
pnpm tauri dev
```

应该能看到类似的日志：
```
Adding clipboard item: type=text, content_len=20
Clipboard item added successfully. Total items: 1
```

## 📝 已知限制

当前版本：
- ✅ 支持文本复制
- ⏳ 图片支持（UI 已准备，需要后端实现）
- ⏳ 视频支持（UI 已准备，需要后端实现）
- ⏳ 100MB 大小限制（需要实现）

## 🔧 故障排除

### 问题：复制后不显示
1. 检查终端是否有错误日志
2. 确认 `pbpaste` 命令可用（macOS）
3. 尝试手动运行：`pbpaste` 看是否有输出

### 问题：重复的记录
- 已优化：只检查最近 5 条记录，避免过度检查

### 问题：监控不工作
- 检查是否有权限问题
- 确认插件已打开（监控只在插件打开时运行）
